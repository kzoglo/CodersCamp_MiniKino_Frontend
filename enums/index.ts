export enum ClassesToManipulate {
  INVISIBLE = 'invisible',
  VISIBLE = 'visible',
  SHOW = 'show',
  HIDE = 'hide',
  GRID = 'grid',
  CURSOR_POINTER = 'cursor-pointer',
  SUBMIT_BTN = 'submitBtn'
}

export enum TimerValues {
  TIME_TO_SHOW_REMINDER = 300000
}
